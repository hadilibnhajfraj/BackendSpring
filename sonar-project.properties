# === Identifiants du projet SonarCloud ===
sonar.projectKey=pidevspringprojects_backendspring
sonar.organization=pidevspringprojects
sonar.host.url=https://sonarcloud.io

# === Code source et compilation ===
sonar.sources=src
sonar.java.binaries=target

# === Exclusions (à affiner selon ta structure) ===
sonar.exclusions=**/test/**, **/target/**, **/*.md, **/*.yml, **/*.yaml

# === Configuration additionnelle recommandée ===
# Désactive les logs trop verbeux
sonar.verbose=false

# Facultatif si vous utilisez GitHub mais que vous ne voulez pas de décorations de PR
# sonar.pullrequest.provider=none
