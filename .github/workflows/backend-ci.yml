name: Test Pipeline Hello World

on:
  push:
    branches: [rafik]
  pull_request:
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Hello World
        run: echo "✅ Hello World depuis GitHub Actions 🎉"